apiVersion: v1
kind: Service
metadata:
  name: mlops-major-service
spec:
  selector:
    app: mlops-major
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
